# build artifacts
build/
*.o
*.app/
*.dSYM/

# data: large downloads + generated binaries (regenerable; see tools/)
data/
*.bin
*.feather
*.tsv
*.csv.gz

# generated reports / screenshots
# (committed docs imagery under docs/ is kept; everything else .png is ignored)
*.png
!docs/*.png
flysim_compare*.html

# python
__pycache__/
*.pyc

# macOS
.DS_Store

# signing secrets (developer-specific)
signing.env

# reusable CI/release recipe — local reference, kept out of the repo
CI-RELEASE-SETUP.md
